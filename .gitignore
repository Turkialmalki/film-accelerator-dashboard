.DS_Store
*.log
node_modules/

# Never commit credentials. This repository is public and GitHub Pages serves
# every file in it, so anything here is world-readable the moment it is pushed.
.env
.env.*

# ...except the template, which holds only names and comments — no values.
# HANDOFF.md has referred to it since the first handoff; it was never actually
# committed, because the rule above swallowed it.
!.env.example

# Next.js
/.next/
/out/
/node_modules/
next-env.d.ts
.env*.local
.env
